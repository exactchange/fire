{
  "name": "fire-backend-example-todo-list-api",
  "author": "@exactchange",
  "description": "A todo list API example with Fire",
  "version": "0.0.3",
  "scripts": {
    "restart": "npm stop && npm start",
    "start": "mongod --fork --syslog && node start",
    "stop": "pkill mongod",
    "test": "node test"
  },
  "dependencies": {
    "fire-backend-test": "^1.0.2"
  }
}
